{
  "name": "express-server",
  "version": "0.0.1",
  "license": "",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && cp -r *.json dist/",
    "start": "node dist/server.js",
    "start:mac": "tsc && cp -r *.json dist/ && node dist/server.js",
    "start:windows": "tsc && xcopy *.json dist\\ /E /I && node dist\\server.js"
  },
  "dependencies": {
    "body-parser": "1.20.1",
    "cors": "^2.8.5",
    "express": "4.18.2",
    "@types/node": "18.11.9"
  },
  "devDependencies": {
    "typescript": "4.8.4"
  }
}
